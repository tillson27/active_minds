{
  "name": "active-minds-contact-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Lambda handler for the ACTive Minds Therapy website contact form. Sends inquiry + auto-reply emails via Resend's REST API.",
  "main": "contact.mjs",
  "scripts": {
    "bundle": "rm -f function.zip && zip -r function.zip contact.mjs emailTemplate.mjs package.json"
  },
  "dependencies": {}
}
