{
  "name": "active-minds-contact-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Lambda handler for ACTive Minds Therapy forms, auto-replies, DynamoDB submissions, and admin access.",
  "main": "contact.mjs",
  "scripts": {
    "bundle": "rm -f function.zip && zip -r function.zip contact.mjs emailTemplate.mjs package.json"
  },
  "dependencies": {}
}
